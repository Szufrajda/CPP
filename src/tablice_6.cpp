#include <algorithm>
#include <iostream>
#include <string>

auto search(int a[], int n) -> int
{
    n = 10;
    int needle;
    for (int i = 1; i < n; ++i) {
        if (a[0] == a[i])
    }
    needle = a[0];
    std::cout << needle << "\n";
    return needle;
    if
        not(a[0] > a[i] || a[0] < a[i]) std::cout << "-1"
                                                  << "\n";
}
auto main() -> int
{
    int tab[10]{42, 9, -1, 18, 59, 3, 101, 32, 72, 12};
    search(tab, 1);
    return 0;
}
